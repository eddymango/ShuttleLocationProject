<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="22dp">

    <TextView
        android:id="@+id/checkdialog_tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:fontFamily="@font/pretendard_medium"
        android:includeFontPadding="false"
        android:text=""
        android:textColor="@color/colorBlack"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/checkdialog_btn_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingEnd="11dp"
        android:fontFamily="@font/pretendard_semibold"
        android:includeFontPadding="false"
        android:paddingStart="13dp"
        android:paddingTop="13dp"
        android:paddingBottom="11dp"
        android:text="확인"
        android:textColor="@drawable/tv_d_buttontext_primary"
        android:textSize="13sp"
        app:layout_constraintTop_toTopOf="@id/checkdialog_btn_cancel"
        app:layout_constraintEnd_toStartOf="@id/checkdialog_btn_cancel"/>

    <TextView
        android:id="@+id/checkdialog_btn_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="11dp"
        android:fontFamily="@font/pretendard_semibold"
        android:includeFontPadding="false"
        android:paddingStart="13dp"
        android:paddingEnd="11dp"
        android:paddingTop="13dp"
        android:paddingBottom="11dp"
        android:text="취소"
        android:textColor="@drawable/tv_d_buttontext_black"
        android:textSize="13sp"
        app:layout_constraintTop_toBottomOf="@id/checkdialog_tv_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="22dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>